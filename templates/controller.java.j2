package {{ package }}.controller;

import {{ package }}.controller.base.BaseController;
import java.util.List;
import {{ package }}.dal.entitys.{{ table.class_name }};
import {{ package }}.service.{{ table.class_name }}Service;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiImplicitParam;
import io.swagger.annotations.ApiImplicitParams;
import io.swagger.annotations.ApiOperation;
import org.apache.dubbo.config.annotation.DubboReference;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import {{ package }}.controller.{{ table.class_name|lower }}.constants.LabelUrl;

@RestController
@RequestMapping({{ table.class_name}}Url)
public class {{ table.class_name }}Controller extends BaseController {

    @DubboReference(time)
    private {{ table.class_name }}Service service;

    @PostMapping()
    public {{ table.class_name }} detail(@PathVariable Long id) {
        return service.selectById(id);
    }

    @PostMapping()
    public List<{{ table.class_name }}> list() {
        return service.selectAll();
    }

    @PostMapping()
    public void add(@RequestBody {{ table.class_name }} entity) {
        service.insert(entity);
    }

    @PostMapping()
    public void update(@RequestBody {{ table.class_name }} entity) {
        service.update(entity);
    }

    @PostMapping()
    public void delete(@PathVariable Long id) {
        service.deleteById(id);
    }
}
